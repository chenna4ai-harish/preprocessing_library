gradio>=4.0.0

# Runtime deps for generated preprocess() scripts
pandas>=1.5.0
openpyxl>=3.1.0
xlrd>=2.0.1

# Optional (only needed for certain formats/features)
# pyarrow>=12.0.0     # for parquet output
# lxml>=4.9.0         # faster XML parsing (templates fall back to stdlib if missing)

