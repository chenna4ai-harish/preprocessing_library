# Preprocessing Script Library — pinned to personal laptop (working) versions
# Install: pip install -r requirements.txt
#
# Python 3.10.x recommended (tested with 3.10.18 Anaconda)

# ── Core UI ───────────────────────────────────────────────────────────────────
gradio==6.3.0

# ── Gradio internals (pin starlette — v1.0.0 breaks Gradio 6.x queue) ────────
fastapi==0.128.0
uvicorn==0.40.0
starlette==0.50.0
httpx==0.28.1
anyio==4.10.0

# ── Data processing ───────────────────────────────────────────────────────────
pandas==2.2.3
numpy==2.2.6
openpyxl==3.1.5        # .xlsx support
xlrd==2.0.2            # .xls (old Excel) support
pyarrow==21.0.0        # .parquet support
